[project]
name = "gencal"
version = "2.0.0"
description = "Flask app that serves a calendar as an SVG image"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "svgwrite>=1.4.3",
]
